import './AnswerList.css';

// TODO: Implement AnswerList to display all answers for a question, each with content,
// author, posted date, and VoteButtons. Show an empty-state message when there are no answers.
const AnswerList = ({ answers }) => {
  return null;
};

export default AnswerList;